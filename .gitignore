# envs & build
.venv/
*.egg-info/
build/
dist/
__pycache__/
*.pyc

# VARD per-repo index/caches (regenerated)
.vard/

# local secrets / config
.mcp.json
*.key

# os
.DS_Store

# local agent config (machine-specific hook paths)
.claude/
.gstack/
VARD_REPORT.pdf
VARD_REPORT.md
VARD_REPORT.html

# JVM agent: ship source + pom + build.sh; the shaded jar (4MB, bundles ByteBuddy) is a build
# artifact rebuilt via `bash vard-agent/build.sh` — don't churn git history with it.
vard-agent/target/
vard-agent/vard-agent.jar
vard-agent/dependency-reduced-pom.xml
