# docs/

Put the usage demo here as `usage.gif`, then uncomment the image line near the top of the
root `README.md`.

Suggested 10–15s recording (terminal):

```bash
vard init .
vard context "the status page shows stale data"
```

The shot to capture is the **"Coupled through shared data"** section — the writer⇄reader link
through a cache key that grep/embeddings miss. That is the one-glance "why VARD" moment.

Tools: `asciinema` + `agg` (asciinema → gif), or any screen recorder exported to GIF.
Keep it small (≤ ~720px wide, a few hundred KB).
